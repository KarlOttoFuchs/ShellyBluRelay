# ESP32-C3 Relay Module - SDK Configuration Defaults
# Story 2.1: Enable NimBLE for BLE scanning

# Enable Bluetooth
CONFIG_BT_ENABLED=y

# Enable NimBLE (disable Bluedroid)
CONFIG_BT_NIMBLE_ENABLED=y
CONFIG_BT_BLUEDROID_ENABLED=n

# BLE controller mode - BLE only
CONFIG_BTDM_CTRL_MODE_BLE_ONLY=y
CONFIG_BTDM_CTRL_MODE_BR_EDR_ONLY=n
CONFIG_BTDM_CTRL_MODE_BTDM=n

# NimBLE specific settings
# No connections needed - we're only scanning/observing
CONFIG_BT_NIMBLE_MAX_CONNECTIONS=0
CONFIG_BT_NIMBLE_ROLE_CENTRAL=n
CONFIG_BT_NIMBLE_ROLE_PERIPHERAL=n
CONFIG_BT_NIMBLE_ROLE_BROADCASTER=n
CONFIG_BT_NIMBLE_ROLE_OBSERVER=y

# Memory allocation
CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_INTERNAL=y
